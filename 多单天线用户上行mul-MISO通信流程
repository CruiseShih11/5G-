# 多单天线用户上行mul-MISO通信流程

## 前言

写这篇帖子的动机是：对于很多通信专业的同学来讲，虽然我们总是在讨论着各种各样的通信术语：信道估计，波束成形，波束聚焦，预编码等。但是目前大多数论文与教材中对于整个过程的流程与逻辑并没有深入地提及，因为大多数论文都在研究其中的某一个子问题，进而对这个子问题进行建模如：信道估计中的$y=AH+N$。但是这个建模是如何得到的呢？真实的天线接收到的是什么呢？什么时候进行$IFFT/FFT$的$OFDM$变换呢？这个相应的硬件部分在哪里呢？诚然，不知晓这些具体的问题对于研究单个问题，并不会有很大的影响，但是这对于学生想进一步地深入研究通信系统有巨大的阻碍。笔者根据自己的所学结合各种工具包括AI等，对这一部分内容进行整理，如有错误与补充，请各位$github$上的朋友进行指正。

![整个阵列通信流程的模型](E:\系统默认\桌面\文献书籍\5G通信流程等\整个阵列通信流程的模型.png)

## UE手机端的信号发送

发送的所有信号都是模拟信号生成的电磁波，4G与5G中通过引入了正交频分复用(OFDM)技术，有效地拓展了每个符号的时域宽度(将时域宽度扩展了$N_{sub}$倍)，同时各个子载波相互正交，因此每个子载波都可以看做为一个窄带信号，进而将频率选择性衰落转化为平坦衰落，如下图所示。

<img src="E:\系统默认\桌面\文献书籍\5G通信流程等\image-20260401204357933.png" alt="image-20260401204357933" style="zoom:50%;" />

$$y_i(t) = h_i(t) * x(t) + n(t)$$ 

是最底层的物理规律。对于**任何**信号（无论是极窄的窄带，还是几十 GHz 的超宽带），只要无线信道是一个线性系统，接收天线在时域上抓取到的信号，**永远**是发送信号与信道冲激响应的线性卷积。

既然这个宽带公式在物理上是绝对成立的，那为什么我们在 4G/5G 甚至 6G 中，还要大费周章地搞 OFDM，把信道切成窄带呢？

最核心的原因是：**工程师在硬件上“解不开”或者说“算不起”这个时域卷积。**

我们可以从以下两个维度来看看，如果直接用你这个公式去处理宽带信号，会发生什么灾难：

### 1. 码间干扰 (ISI) 的物理灾难

宽带信号意味着极高的数据传输速率，这导致每一个数据符号在时域上的持续时间（$T_s$）非常非常短。

- **物理现实：** 城市环境中的多径效应会产生回声，最长路径和最短路径到达接收端的时间差（最大多径时延 $\tau_{max}$）往往在几微秒级别。
- **当带宽变宽时：** 符号时间 $T_s$ 会变得远小于 $\tau_{max}$。这意味着，基站发送的第 1 个符号的回声还没在空气中消散，第 2 个、第 3 个甚至第 10 个符号就已经到达接收端了。
- **结果：** 这些符号在时域上严重重叠，发生了剧烈的**码间干扰 (Intersymbol Interference, ISI)**。在示波器上看，此时的 $y_i(t)$ 就是一锅彻底糊掉的波形。

### 2. 时域均衡器 (Equalizer) 的算力爆炸

如果我们在基带芯片里硬解这个公式，也就是已知收到的 $y_i(t)$ 和估计出的信道 $h_i(t)$，想要逆推求出发送的 $x(t)$，这个数学过程叫做**反卷积（Deconvolution）**。

1. **早期的妥协：** 在 2G (GSM) 和 3G (WCDMA) 时代，带宽比较窄（几十 kHz 到 5 MHz），ISI 还不算太严重。当时的基带确实是直接在时域硬扛这个公式的。工程师发明了各种**时域均衡器**（比如 Viterbi 算法、判决反馈均衡器 DFE、Rake 接收机）来消除卷积带来的干扰。
2. **宽带的绝望：** 时域均衡器的计算复杂度，是随着多径数量（信道抽头数）呈**指数级**或**高阶多项式级**增长的。到了 4G/5G 时代，信道带宽动辄 100 MHz 甚至更宽，如果在时域硬解反卷积，所需要的乘法器数量和功耗，会直接烧毁任何现有的手机基带芯片。没有任何硬件能在微秒级的时间内算完这么庞大的时域矩阵求逆。

### 总结：OFDM 的“降维打击”

面对宽带信号的 $y_i(t) = h_i(t) * x(t) + n(t)$，OFDM 采取了一种“逃避可耻但极其有用”的策略：

它不与卷积正面硬刚。它利用傅里叶变换的数学性质（时域的圆周卷积等于频域的乘法），外加一个极其巧妙的物理设计（插入循环前缀 CP），硬生生地把这个极其复杂的时域方程，等价映射成了频域上成百上千个极其简单的标量方程：

$Y[k] = H[k] \cdot X[k] + N[k]$

| **维度**     | **时域处理 (直接刚宽带)**                  | **频域处理 (OFDM化整为零)**           |
| ------------ | ------------------------------------------ | ------------------------------------- |
| **底层公式** | $y_i(t) = h_i(t) * x(t) + n(t)$            | $Y[k] = H[k] \cdot X[k] + N[k]$       |
| **数学操作** | **反卷积 (极其复杂、耗电)**                | **复数除法 $X = Y / H$ (小学生级别)** |
| **硬件代价** | 巨大的时域抽头延迟线均衡器，算力随带宽爆炸 | 仅需一套高效的 FFT/IFFT 硬件加速引擎  |
| **时代应用** | 2G (GSM), 3G (WCDMA/CDMA2000)              | 4G (LTE), 5G (NR), Wi-Fi 4/5/6/7      |



<img src="E:\系统默认\桌面\文献书籍\5G通信流程等\image-20260401204451235.png" alt="image-20260401204451235" style="zoom:50%;" />

<img src="E:\系统默认\桌面\文献书籍\5G通信流程等\image-20260401204532414.png" alt="image-20260401204532414" style="zoom:50%;" />



<img src="E:\系统默认\桌面\文献书籍\5G通信流程等\image-20260401204702733.png" alt="image-20260401204702733" style="zoom:50%;" />

<img src="E:\系统默认\桌面\文献书籍\5G通信流程等\image-20260401204746126.png" alt="image-20260401204746126" style="zoom:50%;" />



## 经过信道的过程

之所以在某个特定的子载波上，信道可以被极其简单地表示为一个 $N \times 1$ 的复数向量 $\mathbf{h}$，正是因为 **傅里叶变换（FFT/IFFT）将时域上极其复杂的“多径卷积”效应，转化为了频域上极其简单的“标量乘法”**。

我们可以通过严谨的数学推导，来看看这个“转化”是如何发生的。

------

### 1. 时域上的残酷现实：多径与卷积

在现实的物理世界中，发射端（手机）发出的信号，会经过直射、建筑物反射、地面折射等多条路径（Multi-path）到达基站。每条路径都有自己的**衰减幅度**和**到达延迟**。

对于基站的第 $i$ 根天线，它在时域上感受到的信道冲激响应 $h_i(t)$ 可以表示为一个多径模型（Tapped-Delay Line）：

$$h_i(t) = \sum_{l=1}^{L} \alpha_{i,l} \delta(t - \tau_{i,l})$$

其中：

- $L$ 是总的多径数量。
- $\alpha_{i,l}$ 是第 $l$ 条路径到达第 $i$ 根天线时的复数衰减（包含幅度和初始相位）。
- $\tau_{i,l}$ 是第 $l$ 条路径的到达时延。

由于多径的存在，接收到的时域信号 $y_i(t)$ 是发送信号 $x(t)$ 与信道 $h_i(t)$ 的**线性卷积（Convolution）**，这会导致严重的码间干扰（ISI）：

$$y_i(t) = h_i(t) * x(t) + n(t)$$

------

### 2. 频域的魔法：从“频率选择性”到“平坦衰落”

为了解决卷积带来的麻烦，我们在发射端加入了**循环前缀（CP, Cyclic Prefix）**，并在接收端使用了 **FFT**。

（*数学前提：只要 CP 的长度大于最大多径时延，线性卷积就会被等效为圆周卷积。*）

根据信号与系统理论，**时域的圆周卷积，等于频域的直接相乘**。

我们对第 $i$ 根天线的时域信道 $h_i(t)$ 做傅里叶变换，将其映射到频域 $H_i(f)$：

$$H_i(f) = \int \left( \sum_{l=1}^{L} \alpha_{i,l} \delta(t - \tau_{i,l}) \right) e^{-j 2\pi ft} dt$$

利用冲激函数 $\delta$ 的性质，积分结果变为：

$$H_i(f) = \sum_{l=1}^{L} \alpha_{i,l} e^{-j 2\pi f \tau_{i,l}}$$

**注意看这个公式**：它表明在频域上，信道变成了一个随频率 $f$ 连续波动的复数函数。这就是所谓的**频率选择性衰落（Frequency-selective Fading）**。

### 3. 定格在特定的子载波 $k$ 上

现在，OFDM 技术将整个宽带划分成了很多个非常窄的子载波。假设我们只关注第 $k$ 个子载波，它的中心频率是 $f_k$。

我们将 $f = f_k$ 代入上面的公式：

$$H_i[k] = \sum_{l=1}^{L} \alpha_{i,l} e^{-j 2\pi f_k \tau_{i,l}}$$

奇迹出现了！

在这个特定的频点 $f_k$ 上，右边的一长串多径求和（包括各种幅度 $\alpha$ 和相位 $e^{-j...}$），最终**只合并成了一个单独的复数值（标量）**。

$$h_{i,k} = H_i[k]$$

我们说在这个很窄的子载波带宽内，信道是“平坦”的（Flat Fading）。**在这个子载波上，时域的复杂卷积被彻底退化为了简单的“复数乘法”。**

------

### 4. 扩展到 $N$ 根天线：从标量到向量

现在，把单根天线推广到 $N$ 根接收天线的 MIMO 阵列。

在同一个特定的子载波 $k$ 上，每根天线 $i$ 都会对应计算出一个标量信道 $h_{i,k}$。

如果把这 $N$ 根天线在这个子载波 $k$ 上感受到的**平坦衰落标量**排成一列：

$$\mathbf{h}_k = \begin{bmatrix} h_{1,k} \\ h_{2,k} \\ \vdots \\ h_{N,k} \end{bmatrix}$$

这个 $\mathbf{h}_k$ 就是你在看论文或写代码时经常碰到的那个 $N \times 1$ 的复数向量。这就是它之所以能这么简单、这么优雅的原因。





## 阵列天线接收端

### 经过$W_{RF}$的处理与ADC,FFT的处理

在天线和射频前端，真实发生的事情是**时域**的。真实的频域信息不是像前面信道中表示的那样，而是我们每一个天线接收到$y_i(t)$之后经过$W_{RF}$处理。

 $y_{ant}(t)=\begin{bmatrix} y_1(t) \\ y_2(t) \\ \vdots \\ y_N(t) \end{bmatrix}$

时域信号中包含着多个子载波的信息。

对于连续的时域信号 $y_{ant}(t)$，经过移相器网络后，得到时域的射频输出 $y_{RF}(t)$：

$$y_{RF}(t) = \mathbf{W}_{RF}^H \cdot y_{ant}(t)$$

接着，这个信号进入 ADC 采样并做 FFT。我们用 $\mathcal{F}\{\cdot\}$ 表示 FFT 操作，那么基带拿到的频域信号应该是：

$$\mathbf{y}_{BB}[k] = \mathcal{F}\big\{ y_{RF}(t) \big\} = \mathcal{F}\big\{ \mathbf{W}_{RF}^H \cdot y_{ant}(t) \big\}$$

#### 数学层面的等效魔法（交换律）

关键点来了！模拟移相器矩阵 $\mathbf{W}_{RF}$ 对于整个宽带信号而言，通常被假设为**平坦的（Frequency-flat）**——即无论哪个频率成分经过这个移相器，被改变的相位 $\theta$ 都是一样的。

既然 $\mathbf{W}_{RF}^H$ 是一个与时间 $t$ 和频率 $f$ 都无关的常数复矩阵，而傅里叶变换 $\mathcal{F}$ 是一个**线性变换**。根据线性代数的性质，**常数矩阵乘法可以提到傅里叶变换的外面！**

即：先乘矩阵再做 FFT，等于先做 FFT 再乘矩阵。

$$\mathcal{F}\big\{ \mathbf{W}_{RF}^H \cdot y_{ant}(t) \big\} \equiv \mathbf{W}_{RF}^H \cdot \mathcal{F}\big\{ y_{ant}(t) \big\}$$

 $\mathcal{F}\big\{ y_{ant}(t) \big\}$实际上经过ADC和FFT变换就可以得到多个子载波下多个天线阵元下的信道矩阵。

$\mathcal{F}\{ \mathbf{y}_{ant}(t) \}$ **确实可以看作一个维度为 $N \times K_{sub}$ 的大型矩阵**（其中 $K_{sub}$ 是子载波总数）。

##### 1. 物理层面的“三维视角”

在混合预编码的接收端，数据流实际上经历了一个从 **“空间-时间”** 到 **“空间-频率”** 的变换过程。

- **天线端（时域）：** $N$ 根天线在连续采样，每一时刻 $t$ 产生一个 $N \times 1$ 的向量。
- **模拟域操作：** $\mathbf{W}_{RF}^H$ 在时域实时地将 $N$ 路信号压成 $N_{RF}$ 路信号。
- **数字基带（频域）：** 经过 FFT 后，我们得到了一个 **$N_{RF} \times K_{sub}$** 的复数矩阵。

------

##### 2. 矩阵维度的演变推导

按照你的逻辑，我们来看每一阶段的“数据全貌”：

###### 第一阶段：模拟射频压缩（全带宽并进）

由于 $\mathbf{W}_{RF}^H$ 是由移相器组成的硬件，它对时域信号是“来者不拒”的。

$$\mathbf{Y}_{RF}(t) = \mathbf{W}_{RF}^H \cdot \mathbf{Y}_{ant}(t)$$

- $\mathbf{Y}_{ant}(t)$：维度 $N \times (\text{采样点数})$。
- $\mathbf{Y}_{RF}(t)$：维度 $N_{RF} \times (\text{采样点数})$。

而 $\mathcal{F}\big\{ y_{ant}(t) \big\}$ 正是天线接收信号在第 $k$ 个子载波上的频域表达 $\mathbf{y}_{ant}[k]$。

所以等式完美闭环：

$$\mathbf{y}_{BB}[k] = \mathbf{W}_{RF}^H \cdot \mathbf{y}_{ant}[k]$$

###### 第二阶段：FFT 带来的“空间-频率”矩阵

现在进入 ADC 和 FFT。正如你所说，FFT 是在 $N_{RF}$ 条射频链上**并行**做的。对于每一条射频链，它都把一段时域信号切成了 $K_{sub}$ 个子载波。

#### 为什么要这样等效表达？

在写论文或推导算法时，如果我们在时域去描述 $\mathbf{W}_{RF}$，然后又要到频域去描述信道 $\mathbf{h}_k$ 和数字基带 $\mathbf{W}_{BB}$，整个数学模型会变得极其割裂和复杂。

由于上述的**线性交换律**成立，我们可以理直气壮地把物理上发生在时域的 $\mathbf{W}_{RF}$ 操作，**数学等效地“搬运”到频域的每一个子载波上**。

这样一来，在任意一个子载波 $k$ 上，我们就能写出极其连贯、统一的端到端频域方程：

$$\mathbf{z}[k] = \mathbf{W}_{BB}^H[k] \cdot \mathbf{W}_{RF}^H \cdot \big( \mathbf{h}[k]x[k] + \mathbf{n}[k] \big)$$



### 信道估计

#### 1. 信道估计的精确发生位置

**信道估计完全是在“经过 ADC 和 FFT 之后，在计算 $W_{BB}$ 之前”进行的。**

- **时机：** 导频信号（如 4G/5G 中的 CRS, DMRS, CSI-RS）夹杂在用户的数据流中，一起经历了信道 $\mathbf{h}$、模拟矩阵 $\mathbf{W}_{RF}^H$ 的物理叠加，并穿过了 ADC 和 FFT。
- **动作：** 基带芯片在频域（特定子载波上）将这些已知的导频提取出来。因为发送的导频 $a$ 是已知的，接收到的频域数字信号 $\mathbf{y}_{BB}$ 也是已知的，DSP 就可以通过某种算法（如 LS 最小二乘法或 MMSE 最小均方误差）除掉 $a$，暴露出信道的真实状态。
- **目的：** 正如你所说，这一步估计出的结果（无论是完整的 $\mathbf{h}$ 还是降维的 $\mathbf{H}_{eff}$），都会被直接送入下游的矩阵求逆模块，用于计算数字分离矩阵 $\mathbf{W}_{BB}$，从而完成最终的数据分离。

#### 2. 真实 4G/5G 系统中使用的两阶段信道估计与波束管理

**在真实的 5G 混合预编码系统（尤其是毫米波 FR2 频段）中，工业界实际上采用的是“路径二的实用版”与“路径一”的结合体——被称为“两阶段信道估计与波束管理（Two-stage Beam Management）”。**

我们先明确 4G 和 5G Sub-6GHz 的情况：在这些低频段，基站通常采用**全数字架构**（天线数等于射频链数），此时没有 $\mathbf{W}_{RF}$ 的阻碍，基站直接估计出完整的物理信道 $\mathbf{h}$。

但在 **5G 毫米波（采用混合架构）**中，为了在极度欠定的方程组下活下来，3GPP 协议规定了如下的结合方案：

##### **第一阶段：利用“波束扫描”（路径二的降级实用版）确定 $\mathbf{W}_{RF}$**

- 真实的 5G 系统由于计算量和时延的限制，**极少使用**复杂的“压缩感知（CS）”去完美重构高维信道 $\mathbf{h}$。
- 取而代之的是，基站让 $\mathbf{W}_{RF}$ 在预定义的几个“固定方向（码本）”上轮流切换（这就是 5G NR 中的 **SSB 波束扫描**）。
- 手机测量不同方向上的信号强度（RSRP），告诉基站：“你的第 3 号波束最强”。
- 此时，基站就**固定下** $\mathbf{W}_{RF}$ 的状态（对准第 3 号方向）。这就相当于用一种工程上最笨但也最可靠的扫描方式，抓住了高维信道 $\mathbf{h}$ 中能量最大的一条多径。

##### **第二阶段：利用“等效信道估计”（路径一）计算 $\mathbf{W}_{BB}$**

- 当模拟矩阵 $\mathbf{W}_{RF}$ 被固定后，硬件的“通道”就建立好了。
- 此时，手机发送专门的解调参考信号（DMRS）。基站通过这固定的 $N_{RF}$ 条射频链，直接进行常规的频域信道估计。
- 这里估计出来的，就是你说的 **等效信道 $\mathbf{H}_{eff}$**（维度为 $N_{RF} \times K$）。
- 最后，基带利用 $\mathbf{H}_{eff}$ 计算迫零矩阵 $\mathbf{W}_{BB}$，消除这 $K$ 个用户在这 $N_{RF}$ 个通道内的残余干扰。

<img src="E:\系统默认\桌面\文献书籍\5G通信流程等\image-20260402170220577.png" alt="image-20260402170220577" style="zoom:50%;" />

科研人员研究高深的“路径二（压缩感知等）”，是为了在论文里探寻系统的**理论性能天花板**；而通信标准工程师制定“波束扫描 + 等效信道（混合路径）”，则是为了让这套系统能用极低的芯片功耗和成本，在你的手机里**真实落地**。

#### 为什么还需要第一阶段的波束扫描呢

第一阶段的波束扫描（确定 $\mathbf{W}_{RF}$）主要就是为了“物理对准”，从而利用阵列增益极其暴力地放大信号功率。

但在通信底层的物理与数学逻辑中，这个“变大”并非锦上添花，而是**生死攸关**。如果不经历第一阶段的波束扫描，第二阶段的数字基带信道估计根本无从谈起。我们可以从以下三个维度来深刻理解这背后的“残酷现实”：

##### 1. 物理现实的残酷：毫米波的“见光死”与底噪深渊

混合预编码架构主要是为 5G 毫米波（FR2 频段）或未来的 6G 太赫兹频段设计的。在这些超高频段，电磁波的物理特性极其恶劣：

- **巨大的空间衰减：** 相比于低频，毫米波在空气中的路径损耗极大。
- **单天线信噪比极低：** 如果只看基站的某一根单独的天线阵元，它接收到手机发来的信号功率，通常是**远低于接收机热噪声**的（例如单天线 SNR 可能是 -10 dB 甚至更低）。

**如果没有第一阶段的 $\mathbf{W}_{RF}$：**

假设我们直接跳过波束扫描，让 $\mathbf{W}_{RF}$ 随机指向一个方向，或者使用全向波束。此时，微弱的信号直接进入基站的射频前端和 ADC。

因为信号早就被淹没在底噪之下，经过 ADC 量化后，进入数字基带的 $\mathbf{y}_{BB}$ 实际上就是**纯粹的随机噪声**。基带算法再怎么牛，也无法从一堆纯噪声里估计出等效信道 $\mathbf{H}_{eff}$。

**$\mathbf{W}_{RF}$ 的救命作用（阵列增益）：**

当 $\mathbf{W}_{RF}$ 通过扫描“物理对准”了手机，阵列中所有 $N$ 根天线收集到的微弱电磁波，会在模拟移相器中发生**完美的相长干涉（同相叠加）**。

这会产生巨大的**阵列增益（Array Gain，提升 $10\log_{10}(N)$ dB）**。这股强大的物理能量会在信号进入 ADC 之前，硬生生地把信号从底噪的泥潭里“拔”出来，达到 ADC 能够识别的阈值以上。

##### 2. 数学空间的窄门：$\mathbf{H}_{eff}$ 的“漏斗效应”

我们回到那个经典的等式：

$$\mathbf{H}_{eff} = \mathbf{W}_{RF}^H \mathbf{H}$$

- $\mathbf{H}$ 是 $N \times 1$ 的高维物理信道（客观存在，包含所有方向的多径）。
- $\mathbf{W}_{RF}$ 是 $N \times N_{RF}$ 的模拟降维矩阵（主观设计的物理“漏斗”）。
- $\mathbf{H}_{eff}$ 是 $N_{RF} \times 1$ 的等效信道（基带实际能看见的低维世界）。

**为什么必须先扫描？**

你可以把 $\mathbf{W}_{RF}$ 想象成一个只开了一个极小缝隙的“空间漏斗”。

- **如果漏斗对错了方向：** $\mathbf{W}_{RF}$ 在空间上的指向与真实信道 $\mathbf{H}$ 的方向正交（或相差极远）。在数学上，这两个向量的点积 $\mathbf{W}_{RF}^H \mathbf{H} \approx \mathbf{0}$。这意味着真实信道里的能量完全漏不进来。基带去估计一个接近全 0 的 $\mathbf{H}_{eff}$，毫无意义。
- **如果漏斗对准了方向（通过第一阶段波束扫描）：** $\mathbf{W}_{RF}$ 的空间矢量与 $\mathbf{H}$ 完美平行。此时点积结果最大，$\mathbf{H}_{eff}$ 截获了空间中绝大部分的能量。

##### 3. 基带处理的“不可逆性”

你可能会问：既然数字基带 $\mathbf{W}_{BB}$ 也能调幅调相，为什么不能让 $\mathbf{W}_{BB}$ 去对准，非要让 $\mathbf{W}_{RF}$ 去对准？

核心原因在于：**ADC 是一道单向的“鬼门关”**。

- $\mathbf{W}_{RF}$ 发生在模拟域（ADC 之前）。它可以把信号放大，然后再送去 ADC 采样。
- $\mathbf{W}_{BB}$ 发生在数字域（ADC 之后）。如果信号在进入 ADC 时因为信噪比太低而被**量化噪声**彻底摧毁，或者因为方向没对准导致根本没进入射频链，那么数字基带拿到的就是一堆毫无信息量的死数据（010101的乱码）。
- **俗话说：“Garbage in, garbage out”。** 数字矩阵 $\mathbf{W}_{BB}$ 只能处理进入基带的信号，它无法“无中生有”地找回在模拟射频端就已经丢失的物理能量。

真实的 5G 混合预编码流程是一个极其严谨的**“先粗后精、先硬后软”**的接力赛：

1. **第一阶段（探照灯找人）：** 用 $\mathbf{W}_{RF}$ 进行波束扫描。目标是极其纯粹的——不管干扰，只管把天线的物理增益拉满，确保有一股足够强劲的模拟信号“冲”过 ADC，活着进入基带芯片。
2. **第二阶段（手术刀剔肉）：** 基带拿到这股强劲但可能混杂着其他用户干扰的信号后，再利用这固定的通道进行导频信道估计，算出 $\mathbf{H}_{eff}$，最后用数学矩阵 $\mathbf{W}_{BB}$ 把干扰剔除干净。

<img src="E:\系统默认\桌面\文献书籍\5G通信流程等\image-20260402171646099.png" alt="image-20260402171646099" style="zoom:50%;" />

### 经过$W_{BB}$的处理

**数字基带域 $\to \mathbf{W}_{BB}$ 发挥作用**

基带芯片用纯数学的乘法，将刚才得到的 $\mathbf{y}_{BB}$ 乘以数字合并矩阵 $\mathbf{W}_{BB}^H$：

$$\mathbf{z} = \mathbf{W}_{BB}^H \mathbf{y}_{BB}$$

最终，这 $N_{RF}$ 维的数据，被提纯并分离成了 $K$ 个用户的独立数据流 $\mathbf{z}$（$K$ 维）

为了让推导最清晰，我们设定一个最精简但完全具备代表性的**两用户系统 ($K=2$)**：

- **发送端：** 用户 1 发送信号 $x_1$，用户 2 发送信号 $x_2$。
- **接收端（基站）：** 拥有 $N$ 根天线，但只有 $N_{RF} = 2$ 条射频链。
- **目标：** 从射频链输出的、混杂着两人信号的 $\mathbf{y}_{BB}$ 中，纯净地提取出 $z_1 = x_1$ 和 $z_2 = x_2$。

*(注：为了突出“分离”的核心逻辑，推导中我们暂时忽略加性噪声 $\mathbf{n}$)*

#### 第一步：模拟射频域的“混合” (The Entanglement)

信号在空气中传播并被 $N$ 根天线接收。经过物理移相器网络 $\mathbf{W}_{RF}$（维度 $N \times 2$）后，信号被压缩成了 2 路射频信号，进入 ADC 变成了数字基带信号 $\mathbf{y}_{BB}$。

根据我们之前的结论，这可以等效为一个 $2 \times 2$ 的**等效信道矩阵 $\mathbf{H}_{eff}$**：

$$\mathbf{y}_{BB} = \mathbf{W}_{RF}^H [\mathbf{h}_1, \mathbf{h}_2] \begin{bmatrix} x_1 \\ x_2 \end{bmatrix} = \mathbf{H}_{eff} \begin{bmatrix} x_1 \\ x_2 \end{bmatrix}$$

我们将这个 $2 \times 2$ 的等效矩阵 $\mathbf{H}_{eff}$ 展开，设其内部的复数增益为 $a, b, c, d$：

$$\mathbf{H}_{eff} = \begin{bmatrix} a & b \\ c & d \end{bmatrix}$$

那么，经过 ADC 后，基带处理器拿到的两条数据流 $\mathbf{y}_{BB}$ 具体是什么呢？

$$\begin{bmatrix} y_{BB,1} \\ y_{BB,2} \end{bmatrix} = \begin{bmatrix} a & b \\ c & d \end{bmatrix} \begin{bmatrix} x_1 \\ x_2 \end{bmatrix}$$

展开得到物理现实：

1. **通道 1 的数据：** $y_{BB,1} = a \cdot x_1 + b \cdot x_2$
2. **通道 2 的数据：** $y_{BB,2} = c \cdot x_1 + d \cdot x_2$

**这就是分离前的状态！**

在通道 1 里，用户 1 的信号 $x_1$ 被系数 $a$ 放大，但同时混入了用户 2 的干扰 $b \cdot x_2$。如果我们直接把 $y_{BB,1}$ 当作用户 1 的数据去解调，这个巨大的干扰会导致误码率爆表。

#### 第二步：数字基带域的“分离” (The Separation)

现在，数据来到了纯数字芯片中。基带算法（例如迫零算法，Zero-Forcing, ZF）的任务，就是设计一个 $2 \times 2$ 的数字合并矩阵 $\mathbf{W}_{BB}^H$，乘在 $\mathbf{y}_{BB}$ 上，把干扰消掉。

要让分离后的信号 $\mathbf{z} = \mathbf{x}$，在数学上我们需要：

$$\mathbf{z} = \mathbf{W}_{BB}^H \mathbf{y}_{BB} = \mathbf{W}_{BB}^H \mathbf{H}_{eff} \mathbf{x} = \mathbf{x}$$

显然，**我们要让 $\mathbf{W}_{BB}^H \mathbf{H}_{eff} = \mathbf{I}$（单位矩阵）**。

所以，迫零算法的核心就是求**等效信道矩阵**的逆矩阵：

$$\mathbf{W}_{BB}^H = \mathbf{H}_{eff}^{-1}$$

根据线性代数求 $2 \times 2$ 逆矩阵的公式，数字合并矩阵被配置为：

$$\mathbf{W}_{BB}^H = \frac{1}{ad-bc} \begin{bmatrix} d & -b \\ -c & a \end{bmatrix}$$

#### 第三步：见证奇迹的时刻（相消干涉的数学证明）

现在，基带芯片用算出来的 $\mathbf{W}_{BB}^H$ 去乘以收到的混杂数据 $\mathbf{y}_{BB}$。我们来推导通道 1 的输出 $z_1$：

$$\begin{bmatrix} z_1 \\ z_2 \end{bmatrix} = \frac{1}{ad-bc} \begin{bmatrix} d & -b \\ -c & a \end{bmatrix} \begin{bmatrix} (ax_1 + bx_2) \\ (cx_1 + dx_2) \end{bmatrix}$$

我们单独把第一行（即发给用户 1 的解调数据 $z_1$）拿出来乘开：

$$z_1 = \frac{1}{ad-bc} \Big[ d \cdot (ax_1 + bx_2) - b \cdot (cx_1 + dx_2) \Big]$$

展开括号：

$$z_1 = \frac{1}{ad-bc} \Big[ ad\cdot x_1 + \mathbf{db\cdot x_2} - bc\cdot x_1 - \mathbf{bd\cdot x_2} \Big]$$

**看加粗的部分！**

来自通道 1 的干扰 $\mathbf{+db \cdot x_2}$，与我们通过数字矩阵人为从通道 2 借过来的反向干扰 $\mathbf{-bd \cdot x_2}$，**在数学上极其完美地抵消了（得分为 0）！** 这就是你在前几个问题中问到的**“零陷（Nulling）”**在数字域的绝对体现。

剩下的部分合并同类项：

$$z_1 = \frac{1}{ad-bc} \Big[ (ad - bc) \cdot x_1 \Big] = x_1$$

同理，你可以算出 $z_2 = x_2$。

进而得到两个用户的单独的数据流。
